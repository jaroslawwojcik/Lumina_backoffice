import { Alert, Box, Button, CircularProgress, Stack, Typography } from '@mui/material'
import { Navigate, Outlet, useLocation } from 'react-router-dom'
import { useAuth } from './useAuth'
export function RequireAuth() { const { isLoading, retryStaffContext, signOut, staffAccess, user } = useAuth(); const location = useLocation(); if (isLoading) return <Stack sx={{ alignItems: 'center', justifyContent: 'center', minHeight: '100vh' }}><CircularProgress /></Stack>; if (!user) return <Navigate replace to="/login" state={{ from: location }} />; if (staffAccess === 'authorized') return <Outlet />; const forbidden = staffAccess === 'forbidden'; return <Box component="main" sx={{ alignItems: 'center', display: 'flex', justifyContent: 'center', minHeight: '100vh', p: 3 }}><Stack spacing={3} sx={{ maxWidth: 520 }}><Typography variant="h4">{forbidden ? 'Brak dostępu do panelu' : 'Nie można sprawdzić dostępu'}</Typography><Alert severity={forbidden ? 'warning' : 'error'}>{forbidden ? 'To konto nie ma uprawnień do korzystania z panelu operacyjnego.' : 'Spróbuj ponownie. Jeśli problem się powtarza, skontaktuj się z administratorem.'}</Alert>{!forbidden && <Button onClick={retryStaffContext} variant="contained">Spróbuj ponownie</Button>}<Button color="inherit" onClick={() => void signOut()}>Wyloguj</Button></Stack></Box> }
